global:
  resolve_timeout: 5m

route:
  group_by: ["alertname"]
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: "discord_webhook"

receivers:
  - name: "discord_webhook"
    discord_configs:
      - webhook_url: "${DISCORD_WEBHOOK_URL}"
